export const metadata = {
  title: "About Us | Booking App",
  description: "Learn more about our Booking App and what we offer.",
};

export default function AboutLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}

import React from "react";

export const metadata = {
  title: "Contact Us | Booking App",
  description: "Get in touch with our team for support or inquiries.",
};

export default function ContactLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}

import React from "react";

export const metadata = {
  title: "Signup | Booking App",
  description: "Access your Booking App account to manage and view your bookings.",
};

export default function SignupLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}

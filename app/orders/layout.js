import React from "react";

export const metadata = {
  title: "Orders | Booking App",
  description: "Check your order history and track current orders.",
};

export default function OrdersLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}

import React from "react";

export const metadata = {
  title: "Bookings | Booking App",
  description: "View and manage your current and past bookings easily.",
};

export default function BookingLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
